export interface Cart {
  productId: string;
  productImg: string;
  productName: string;
  productSize: string;
  productColor: string;
  productPrice: number;
  productQuantity: number | undefined;
}
